apply plugin: 'com.android.library'
apply plugin: 'kotlin-android'

apply from: '../ktlint.gradle'

android {
    namespace = "com.ewingsa.ohyeah.helpers"
}

dependencies {
    implementation "$deps.exif_interface"

    testImplementation "$deps.junit"
}
